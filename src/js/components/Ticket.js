import React, { Component } from 'react';
import { view } from 'react-easy-state';
// import state from '../state/State';

class Ticket extends Component {
    render() {
        return (
            <>  

            </>
        )
    }
}

export default view(Ticket);